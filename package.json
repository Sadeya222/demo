{
  "name": "desidude",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "description": "Zero-database video streaming & cataloging site. The GitHub repo (JSON files in src/content/videos/) is the database.",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "npm run build && npx wrangler deploy",
    "deploy:pages": "npm run build && npx wrangler pages deploy dist --project-name=desidude",
    "deploy:cf": "npm run deploy"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "astro": "7.3.2",
    "tailwindcss": "^4.3.3"
  }
}
